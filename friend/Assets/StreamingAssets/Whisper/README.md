# Whisper Models

Place a local multilingual whisper.cpp model here for Phase 2 testing.

Expected default path:

```text
Assets/StreamingAssets/Whisper/ggml-small.bin
```

The model `.bin` files are intentionally ignored by Git because they are large runtime artifacts.
